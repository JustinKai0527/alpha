#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    
    int maxArea(vector<int>& height) {
        int MaxWidth = height.size();    
        for(int i=0; i<MaxWidth; i++){

        }
    }
};